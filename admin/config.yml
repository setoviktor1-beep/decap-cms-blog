backend:
  name: github
  repo: your-username/decap-cms-blog # CHANGE THIS to your repo
  branch: main
  base_url: https://your-worker-url.workers.dev # CHANGE THIS to your worker
  auth_endpoint: auth

media_folder: "assets/images"
public_folder: "/assets/images"

collections:
  - name: "posts"
    label: "Blog Posts"
    folder: "posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Description", name: "description", widget: "text"}
      - {label: "Category", name: "category", widget: "select", options: ["Tech", "Life", "Design", "Tutorial"]}
      - {label: "Tags", name: "tags", widget: "list"}
      - {label: "Cover Image", name: "cover", widget: "image"}
      - {label: "Body", name: "body", widget: "markdown"}
